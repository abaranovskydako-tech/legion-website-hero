ЦЕЛЬ: Обновить МНОГОСТРАНИЧНЫЙ сайт LEGION, НЕ превращая его в лендинг/SPA. Сохранить старую карту сайта и ВСЕ существовавшие формы захвата, вернуть их количество не меньше прежнего. Обновить визуал в стиле нового превью (современные цвета/радиусы/тени), но оставить многостраничность и конверсионную архитектуру. Добавить две новые лид-формы: «Заполнить e-бриф» и «Пригласить на тендер / загрузить ТЗ». Увеличить количество коротких форм на страницах. Добавить маску телефона и базовую валидацию. В конце — коммит/пуш/деплой и выдать рабочую ссылку.

───────────────────────────────────────────────────────────────────────────────
1) ОГРАНИЧЕНИЯ И БАЗОВЫЕ ТРЕБОВАНИЯ
- НЕ превращать сайт в один файл. Это МНОГОСТРАНИЧНЫЙ сайт. Сохранить текущие страницы, URL и навигацию.
- НЕ сокращать число форм захвата относительно старой версии. Минимум — столько же, лучше больше (см. п.4).
- Техстек остаётся статическим (HTML/CSS/JS). Если потребуются скрипты — подключать локально (без сборщика).
- Стили: Tailwind CDN допускается, НО без @apply/require. Либо использовать чистый CSS по токенам (см. п.2).

Страницы (ориентир — адаптировать к фактической структуре проекта):
- / (главная), /services (услуги), /pricing или /rates (цены/пакеты), /cases (кейсы), /about (о нас),
  /contacts (контакты), /faq (FAQ), /privacy (политика).

───────────────────────────────────────────────────────────────────────────────
2) ДИЗАЙН-СИСТЕМА (обновить общий стиль)
Добавить в глобальные стили (например, /css/theme.css) токены:
:root {
  --c-bg: #FAFAFA;
  --c-fg: #0F172A; /* neutral-900 */
  --c-muted: #475569; /* neutral-600 */
  --c-emerald: #059669; /* emerald-600 */
  --c-emerald-hover: #047857; /* emerald-700 */
  --shadow-soft: 0 4px 20px rgba(16,24,40,.06);
  --radius-lg: 16px; --radius-xl: 24px;
}
.btn-primary { display:inline-flex;align-items:center;justify-content:center;border-radius:12px;background:var(--c-emerald);color:#fff;padding:.65rem 1rem;font-weight:600;box-shadow:var(--shadow-soft);transition:.2s; }
.btn-primary:hover { background:var(--c-emerald-hover); }
.btn-ghost { display:inline-flex;align-items:center;justify-content:center;border-radius:12px;border:1px solid #E5E7EB;padding:.65rem 1rem;color:var(--c-fg);background:#fff;transition:.2s; }
.card { background:#fff;border:1px solid #E5E7EB;border-radius:var(--radius-lg);box-shadow:var(--shadow-soft); }
.input,.select,.textarea { width:100%;border:1px solid #E5E7EB;border-radius:12px;padding:.6rem .75rem;background:#fff;color:var(--c-fg); }
.input:focus,.select:focus,.textarea:focus { outline:0; box-shadow:0 0 0 4px rgba(16,185,129,.25); }
.badges li { display:inline-flex;align-items:center;gap:.5rem;background:#fff;border:1px solid #E5E7EB;border-radius:999px;padding:.35rem .7rem;box-shadow:var(--shadow-soft); }

Шрифты, радиусы, тени, отступы — в духе предоставленного превью: крупные заголовки, ровные сетки, округления 16–24px, мягкие тени, спокойная палитра нейтралов + изумрудный.

───────────────────────────────────────────────────────────────────────────────
3) ГЛАВНАЯ (вернуть старую «продающую» структуру + новый стиль)
Герой-блок (разделённая компоновка: фото слева, контент + короткая форма справа):
- Фото слева: public/hero_team_adapted.jpg как фоновое: background-position:left center; background-size:cover; border-radius:24px; min-height:560px. НЕ перекрывать текстами/формами.
- Контент справа: заголовок, подзаголовок, бейджи, 2 кнопки, КОРОТКАЯ ФОРМА (имя, телефон, тип работ). 
Тексты:
  H1: «Персонал от 350 ₽/час — на объект за 3 часа или 2 дня»
  P:  «Новосибирск и область. Грузчики, комплектовщики, уборщики и другие роли. Скорость, качество, юридическая чистота, масштабируемость.»
  Бейджи: «24/7», «Медкнижки», «Смена от 6 ч», «72 часа запуск»
  Кнопки: «Рассчитать стоимость», «Смотреть услуги»
Адаптив: на мобильном фото — ПЕРВЫМ блоком на всю ширину, текст+форма — ниже, без наложений.

Далее на главной (как было в старой версии, но в новом стиле):
- Блок цифр: «200+ сотрудников», «72 часа запуск», «99% смен вовремя», «24/7 поддержка».
- Преимущества (3 карточки-иконки): «Быстрый старт», «Полная защита», «Гибкость».
- Мини-калькулятор стоимости (люди, часы смены, пакет → предварительная сумма) + кнопка «Получить точный расчёт» → ОДНА и та же короткая форма/модалка (имя+телефон).
- Пакеты услуг (3 карточки): «Старт», «Пик-сезон», «Постоянный поток» — у каждой кнопка «Запросить расчёт» → та же короткая форма/модалка.
- Социальный пруф: карточки «Яндекс 4.8/5 — 120 отзывов», «Avito — 300+ заказов», «2ГИС — проверенный подрядчик» + кнопка «Смотреть отзывы».
- Нижний CTA (тёмный блок): «Оставить заявку» (открывает ту же короткую форму/модалку), «Позвонить 8-800-700-00-00» (tel:).

ВАЖНО: Все CTA и «Запросить расчёт» ведут к ЕДИНОЙ короткой форме/модалке (см. п.5).

───────────────────────────────────────────────────────────────────────────────
4) СОХРАНИТЬ МНОГОСТРАНИЧНУЮ СТРУКТУРУ + РАССАДИТЬ КОРОТКИЕ ФОРМЫ
На каждой странице должно быть минимум по 1 короткой форме (имя+телефон) ИЛИ ссылке-триггере на общую модалку:
- /services: под списком услуг — карточка «Подберём команду за 10 минут» (имя+телефон).
- /pricing|/rates: в каждой карточке пакета — «Запросить расчёт».
- /cases: вверху после интро «Оставить заявку» (короткая) + внизу дублирующая.
- /about: блок «Задайте вопрос — перезвоним за 10 минут» (короткая).
- /contacts: помимо формы обратной связи — кнопка «Перезвоните мне» (см. п.6.2).
- /faq: под аккордеоном — «Не нашли ответ? Получите консультацию» (короткая).
ИТОГ: точек входа должно получиться БОЛЬШЕ, чем на старом сайте (не меньше 6 на главной и по 1–2 на внутренних).

───────────────────────────────────────────────────────────────────────────────
5) ЕДИНАЯ МОДАЛЬНАЯ ФОРМА (унифицировать)
Сделать одну модалку (id="lead-modal") с полями: name (обяз.), phone (обяз.), worktype (select). Все кнопки «Запросить расчёт/Получить расчёт/Оставить заявку» открывают её. После submit — sendLead('short') + уведомление «Перезвоним в течение 10 минут».

Телефон: inputmode="tel", плейсхолдер «+7 (___) ___-__-__», маска (см. п.7).

───────────────────────────────────────────────────────────────────────────────
6) ДВЕ НОВЫЕ ЛИД-ФОРМЫ
6.1 «Заполнить e-бриф» (многошаговая форма в модалке или /brief)
- 3 шага: 1) Контакты (имя, телефон, email) 2) Объект/задача (тип работ, объём, сменность) 3) Сроки/доп.условия.
- Прогресс (1/3 → 2/3 → 3/3), кнопки «Далее/Назад», на финале — отправка sendLead('ebrief', payload).
- Ссылка на форму: в хедере «Заполнить e-бриф», в футере, и как доп.CTA в /services и /pricing.
- После отправки — страница/модалка «Спасибо» с текстом «Дошлём расчёт в течение 24 часов».

6.2 «Пригласить на тендер / Загрузить ТЗ» (модалка + загрузка файла)
- Поля: имя*, телефон*, email, компания, комментарий (кратко) + file input (accept=".pdf,.doc,.docx,.xls,.xlsx"; max 25MB).
- Отправка: sendLead('tender') + загрузка файла на /api/lead/tender (multipart/form-data). Если бэка нет — показывать заглушку «Файл принят, менеджер свяжется».
- CTA размещение: в хедере, на /services, на /pricing и на /contacts.

БЭКЭНД-МИКРОСЕРВИС (минимал):
- Node.js + Express, маршрут POST /api/lead/ebrief (JSON) и POST /api/lead/tender (multipart).
- Принимать данные (multer для файла), пересылать в Telegram Bot API (sendMessage для e-брифа, sendDocument для ТЗ).
- ENV: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID.
- Деплой как отдельный Web Service на Render, вернуть его URL и прописать в фронтовом fetch().

───────────────────────────────────────────────────────────────────────────────
7) МАСКА ТЕЛЕФОНА И ВАЛИДАЦИЯ (обязательно)
Добавить в общий JS (например, /js/forms.js):
function maskPhone(el){ const t=el; const f=(v)=>{ let s=v.replace(/\D/g,'').slice(0,11); let r='+7 ('; if(!s.startsWith('7')) s='7'+s; r+=s.slice(1,4); if(s.length>4) r+=') '+s.slice(4,7); if(s.length>7) r+='-'+s.slice(7,9); if(s.length>9) r+='-'+s.slice(9,11); return r; }; t.addEventListener('input',()=>t.value=f(t.value)); t.addEventListener('focus',()=>{ if(!t.value) t.value='+7 ('; }); t.addEventListener('blur',()=>{ if(t.value.replace(/\D/g,'').length<11) t.value=''; }); }
document.querySelectorAll('input[type="tel"]').forEach(maskPhone);

Базовая валидация перед отправкой: имя ≥ 2 символа; телефон 11 цифр; email по regex при наличии. Ошибки — подсветка красной рамкой + aria-live сообщение.

───────────────────────────────────────────────────────────────────────────────
8) ЕДИНЫЙ ОТПРАВЩИК ЛИДОВ (front)
В /js/forms.js:
async function sendLead(kind, payload={}, file=null){
  try{
    if(kind==='tender' && file){ const fd=new FormData(); Object.entries(payload).forEach(([k,v])=>fd.append(k,v)); fd.append('file',file); await fetch('https://<YOUR-WEB-SERVICE>.onrender.com/api/lead/tender',{method:'POST',body:fd}); }
    else { await fetch('https://<YOUR-WEB-SERVICE>.onrender.com/api/lead/ebrief',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind,...payload})}); }
    alert('Заявка отправлена. Мы перезвоним в течение 10 минут.');
    window.dataLayer = window.dataLayer || []; window.dataLayer.push({event:'lead_submit', kind});
  }catch(e){ alert('Ошибка отправки. Попробуйте ещё раз или позвоните нам.'); console.error(e); }
}

Все формы/кнопки по сайту вызывают либо общую модалку (короткая форма), либо e-бриф/тендер-модалки, а те — sendLead().

───────────────────────────────────────────────────────────────────────────────
9) ХЕДЕР/ФУТЕР
- Хедер: пункты меню прежние; добавить две кнопки: «Заполнить e-бриф» и